# Copy to .env.local and fill in. Never commit .env.local.
# PRD section 7, plus two additions noted below.

# --- Supabase ---
# Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Bypasses RLS — used by webhooks and the automation runner.
SUPABASE_SERVICE_ROLE_KEY=

# --- Twilio ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
# The dedicated number, E.164 (e.g. +15551234567).
TWILIO_PHONE_NUMBER=
# Not in PRD section 7. Added in step 2: inbound calls are forwarded here.
TWILIO_FORWARD_TO_NUMBER=+15145818570

# --- App ---
# Not in PRD section 7. Optional. The public origin Twilio calls — your ngrok
# URL locally, the deployment URL in production, no trailing slash.
# Leave blank to derive it from forwarded headers, which works under both
# ngrok and Vercel. Set it if signature verification starts failing.
APP_BASE_URL=

# --- Anthropic (step 7) ---
ANTHROPIC_API_KEY=
