import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard · VoltaScales",
};

export default function DashboardPage() {
  return (
    <h1 className="text-lg font-semibold tracking-tight">Dashboard</h1>
  );
}
